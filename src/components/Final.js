import React from 'react'
import '../scss/Final.scss';

const Final = ({message}) => {
    return (
        <div className='final'>
            <small>{message}</small>
        </div>
    )
}

export default Final
